import React from 'react';
import { Text, View } from 'react-native';
const Profile = () => {
    return (
        <View>
            <Text style={{ color: '#000', textAlign: 'center', fontSize: 20 }}>My Profile</Text>
        </View>
    );
};

export default Profile;